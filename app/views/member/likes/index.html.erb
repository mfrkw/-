<div class="container">
  <div class="row">
    <div class="col">
      <% @likes.each do |like| %>
        <%= link_to member_dog_path(like.dog.id) do %>
        <%= attachment_image_tag like.dog, :image, :fill, 150, 150, format: 'jpeg' %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

